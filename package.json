{
  "name": "bookweb",
  "version": "1.0.0",
  "description": "Bookweb",
  "engines": {
    "node": "12.18.4",
    "yarn": "1.22.5"
  },
  "scripts": {
    "install": "(cd api && yarn) & (cd client && yarn)",
    "start": "cd api && yarn start:prod",
    "heroku-postbuild": "cd client && yarn build",
    "debugApi": "cd api && yarn debug",
    "format": "prettier --write",
    "chromatic": "cd client && yarn chromatic"
  },
  "author": "THE BEST",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "husky": "^4.2.5",
    "lint-staged": "^10.2.9",
    "prettier": "^2.0.5"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css,scss,md}": [
      "yarn format"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "devDependencies": {}
}
